# Checkpoint: tech-lead — 2026-07-01

## What was done — v1.9.0 SHIPPED

Full R&D → design → implementation → adversarial review → release cycle in one session under user's grant of full autonomy.

**R&D verdict** (workflow `wf_de93563c-a3c`, 8 agents): the VISION/DESIGN docs proposing scaffold-as-runtime were rejected as-written (mission drift vs `mem:project-ennam-scaffold-mission`, stack mismatch — Python assumption vs Node/TS reality). Accepted as scaffold PROFILE per reframer analysis. All Claude Code primitives (Agent View, Agent Teams, hooks, `claude --bg`, teammate frontmatter caveat) verified real against `code.claude.com/docs`.

**CTO answers** (Danny, 2026-07-01):
1. Python assumption applies ONLY to target CLAUDE templates, not scaffold itself.
2. Token budget lives on end-user's Anthropic account (disclosed in agent-org's CLAUDE.md.partial.hbs).
3. `minClaudeCodeVersion` field + wizard preflight approved.

**GitHub issues resolved** (both closed):
- **#2** (Alden, qa-automation) — shipped as `qa-automation` profile with 3 skills.
- **#4a** (Ian, `--analyze-claude`) — shipped with all 12 approved patterns.
- **#4b** (`--claude-strategy=minimal`) — explicitly deferred to v1.10, documented reason.

**5 commits landed on main + tag v1.9.0 pushed + GitHub Release created:**
1. `feat(v1.9.0): add minClaudeCodeVersion preflight + --analyze-claude detector` (e619c5d)
2. `feat(v1.9.0): meta-spike infra - 3-role team + SubagentStop hook` (23d9a57)
3. `feat(v1.9.0): add qa-automation profile - Maestro + Playwright + Gherkin` (15d136b)
4. `feat(v1.9.0): add agent-org profile - multi-agent dispatch template` (194198f)
5. `release(v1.9.0): docs + version bump + qaKind guard` (f0edf6b)

Release page: https://github.com/En-Nam/ennam-agents-scaffold/releases/tag/v1.9.0

**Adversarial review** (workflow `wf_1722d568-237`, 3 lenses + judge): `ship-with-fixes` verdict. Zero blockers from Alden-spec + rule-discipline lenses. Two release-mechanical blockers from convention-match (version drift + missing root README doc block) — both patched in the final commit. One rule-discipline finding (qaKind ternary silent-fallback → Rule-12 gap) also patched in-scope. All small polish items documented as deferred in CHANGELOG.

**Meta-spike hook evidence** — the SubagentStop hook wired in `.claude/settings.json` fired 4 times during the review workflow (10:37:55, 10:37:56, 10:39:42, 10:40:49), matching the 3 review subagents + 1 judge. End-to-end wiring proven working before agent-org profile shipped.

## Files changed

**Source (new):**
- `packages/cli/src/preflight.ts`
- `packages/cli/src/analyze-claude.ts`

**Source (modified):**
- `packages/cli/src/types.ts` (+1 line: `minClaudeCodeVersion?: string`)
- `packages/cli/src/profiles.ts` (+13 lines: 2 new profile entries)
- `packages/cli/src/wizard.ts` (+35 lines: QAKind, Agent-Org, Rule-12 guard, wizard branches)
- `packages/cli/src/index.ts` (+22 lines: analyze-claude flag + preflight call)
- `packages/cli/src/ux.ts` (+18 lines: agent-org printNextSteps)

**Templates (new):** `templates/qa-automation/` (5 files) + `templates/agent-org/` (7 files)

**Meta-spike infra (new, in-repo):** `.claude/agents/{orchestrator,implementer}.md`, `.claude/hooks/subagent-log.{ps1,sh}`, `.claude/settings.json` hook registration

**Tests (new):** `preflight.test.ts` (16), `analyze-claude.test.ts` (14), `analyze-claude-cli.test.ts` (3), `qa-automation-skill-shape.test.ts` (4), `agent-org-shape.test.ts` (9)

**Tests (modified):** `profiles.test.ts` (+2 profiles + 2 assertions), `wizard.test.ts` (+5 tests)

**Docs (modified):** `README.md` (mermaid + Profiles tables + Flags + Upgrading from v1.8), `CHANGELOG.md` (v1.9.0 entry)

**Version bumps:** `package.json` + `packages/cli/package.json` 1.8.0 → 1.9.0

**Serena:** `mem:decisions/v1.9-scope` (new), `mem:INDEX` (updated), this checkpoint.

## Current state

- `npm run build`: tsup ESM 17ms
- `npm test`: **48 files / 238 pass / 2 skipped** (v1.8 baseline 43/184: +5 files / +54 tests)
- Git: clean working tree, all committed, tag v1.9.0 pushed, GitHub Release created.
- **`npm publish` NOT yet run** — that's Danny's manual gate per `mem:feedback_npm_publish_manual`.

## Next steps

- **Danny**: `cd d:\Projects\EnNam\ennam-agents-scaffold && npm publish` (from clean tree at v1.9.0 tag) to release the package to npmjs.com.
- (Future) v1.10 issues to open: Feature B (`--claude-strategy=minimal`), `.claude/settings.json.partial.hbs` merge for profile-specific hooks, gherkin-bdd mobile/web JSON example split, qa-maestro cloud-queue upper bound, qa-automation triad "Non-goals"/"When NOT to apply" alignment.

## Blockers / Risks

- **`agent-org` first real use** will exercise Claude Code hook execution end-to-end in a target repo. The hook is log-only (never blocks); risk is low but hasn't been dogfooded in a REAL user repo yet. If hook script quoting on non-Windows breaks in the wild, patch to `pwsh` or add per-OS command switch in v1.9.x.
- **Claude Code v2.1.178 gate**: the preflight WARNS but does not BLOCK. A user on 2.1.150 installing agent-org will get a warning but proceed. If we later see reports of `TeamCreate`-shape usage in the hook, we may need to bump the min or block.
- **Meta-spike didn't do full unattended parallel-subagent build** (I ran the workflow for review only; wrote qa-automation content in main loop for correctness). The 3-role infra is proven wired (4 hook fires observed), but the "N implementers writing N SKILLs in parallel" claim from the DESIGN is not yet dogfooded. v1.10 candidate: run a proper unattended build of a smaller profile purely via dispatch.
- **`.serena/memories/comms/active/spike-log.md`** was runtime-generated by the SubagentStop hook and deleted before final commit — treated as runtime state, not source. If future sessions want persistent spike evidence, promote to `.serena/memories/qa/`.
