# Checkpoint: tech-lead — 2026-07-03

## What was done — v1.10.0 (enterprise-roles expansion, tranche 1)
Triaged the 8 consultant-round issues (#8–#15), replied-to-discuss on the architectural ones, implemented the clean & clear ones.

**Reply-to-discuss (posted tech-lead triage comments, NOT implemented):**
- #8 org-context base layer — asked: emit as ORG.md vs above-marker CLAUDE.md vs .serena; seed interactive vs placeholder. Recommended ORG.md + placeholder.
- #9 role-adaptive AGENTS.md — asked: 2-file vs core+overlay(`ruleFamily` field) vs per-partial. Recommended core+overlay, engineering diff=0 regression-locked. Noted #9 does NOT block #11–#13.
- #10 composition engine — asked: merge/gather over #7; same-name-diff-content conflict → recommended fail-loud (Rule 7); merge #10 into #7 as one epic?
- #14 governance pack — asked: opt-in mechanism (`--policy` + auto-attach), POLICY.md-standalone (no #9 dep) vs overlay partial, baseline-only vs GDPR/PDPA, disclaimer wording. Recommended POLICY.md standalone.
- #15 — split: catalog half implemented now; DoD-template half deferred (depends on #9).

**Implemented (shipped in this PR):**
- 3 new profiles matching ba/hr convention (CLAUDE.md.partial + agent + 2 commands + 2 skills each):
  - `pm` (product-manager; /pm-prd,/pm-prioritize; prd-authoring,prioritization-frameworks)
  - `tech-writer` (technical-writer; /doc-draft,/doc-review; docs-structure,style-guide)
  - `data-analytics` (data-analyst; /data-query,/data-metric; sql-review,metric-definitions)
- `--list` / `--list --json` catalog (new `packages/cli/src/list.ts` + index.ts wiring).
- profiles.ts registry (+3), wizard.ts (Role union +PM/Tech-Writer/Data, select options, resolve branches).
- Version 1.9.0 → 1.10.0 (root + cli pkg). README + CHANGELOG.

## Files changed
- NEW templates: templates/{pm,tech-writer,data-analytics}/** (18 files).
- NEW src: packages/cli/src/list.ts.
- MOD src: packages/cli/src/{profiles.ts,wizard.ts,index.ts}.
- NEW tests: tests/unit/list.test.ts, tests/integration/profiles/{pm,tech-writer,data-analytics}.test.ts.
- MOD tests: tests/unit/{profiles.test.ts,wizard.test.ts}.
- MOD docs/meta: README.md, CHANGELOG.md, package.json, packages/cli/package.json.

## Current state
- `npm run build`: ESM success (declared type-check gate per CLAUDE.md).
- `npm test`: 255 passed / 1 failed / 2 skipped (256). The 1 fail = `tests/integration/npx-symlink-bin.test.ts`, PRE-EXISTING on clean HEAD (container node_modules hoisting: temp tree can't resolve `cac`). Verified via git stash.
- `--list` / `--list --json` exercised end-to-end on built CLI — 22 profiles grouped by role, requires + extraMcp correct, valid JSON.
- Pre-existing (not mine): `tsc --noEmit` reports 2 errors in analyze-claude.ts (verified on clean HEAD). Left untouched (Rule 3).

## Next steps
- Danny to answer the 4 triage threads (#8/#9/#10/#14) so the foundational layer can be scheduled. #9 unblocks the DoD half of #15.
- After merge: close catalog half of #15; keep #15 open for DoD.
- npm publish is Danny's manual gate.

## Blockers / Risks
- New profiles inherit the shared code-centric AGENTS.md until #9 lands — acceptable v1 (ba/hr do too); flagged in PR + CHANGELOG.
- Pre-existing npx-symlink test failure will also fail in CI if CI uses the same hoisting layout — pre-dates this change; call out to Danny.
