# Checkpoint: tech-lead — 2026-06-25

## What was done
- Acted as Tech Lead for v1.6.0 feature: wire Anthropic's Superpowers plugin into the scaffold's downstream `.claude/settings.json` so the 14 `superpowers:*` skill references already present in `CLAUDE.md.partial.hbs` resolve out of the box.
- Ran a 6-agent ultracode Workflow (`wf_844e5f4c-1b9`, ~287k tokens): researcher + scaffold-auditor (parallel), 3 advocates (passthrough / fork / overlay), synthesizer. Strict-JSON validated.
- Picked Option A — passthrough to `claude-plugins-official` (Anthropic's auto-registered marketplace). Rejected Option B (`@ennamjsc/superpowers` fork: scope inflation per `mem:project_ennam_scaffold_mission`, premature insurance, ~8-16 hrs/yr fork tax) and Option C (overlay `@ennamjsc/superpowers-extras`: solving tomorrow's problem with today's complexity).
- Wrote decision `mem:decisions/superpowers-plugin-strategy` + linked from `mem:INDEX`.

## Files changed
- **Modified**: `templates/_shared/.claude/settings.json.hbs` — added `enabledPlugins["superpowers@claude-plugins-official"]: true` (3 lines).
- **Modified**: `templates/_shared/.claude/hooks/session-start.{ps1,sh}` — added install-fallback line each.
- **Modified**: `packages/cli/src/ux.ts` — extended `printNextSteps` step list with the trust-prompt + headless `/plugin install` workaround (1 line).
- **Modified**: `.serena/memories/INDEX.md` — linked the new decision memory.
- **Created**: `tests/integration/install-superpowers-settings.test.ts` — 3 tests (fresh install / user-merge / opt-out).
- **Created**: `.serena/memories/decisions/superpowers-plugin-strategy.md` (via Serena MCP).
- **Untouched**: profile templates, agent prompts, all `templates/<profile>/` — the 14 skill references in those files start resolving without any prose edits because upstream Superpowers v6.0.3 ships those exact names.

## Current state
- `npm run build`: ✅ tsup ESM build success (15ms).
- `npm test`: ✅ 39 files / 166 pass / 2 skipped — no regressions. New file `install-superpowers-settings` has 3/3 green.
- Git status: 5 modified + 2 new. Diff stat = `+9 / -2` across the modified files (surgical per Rule 3).
- Dogfood `.claude/settings.json` at repo root deliberately NOT auto-modified (it uses legacy `additionalAllowList` shape and is human-managed; the migration warning in `maybeWarnLegacySettings` already covers it).

## Next steps
- User decides version bump (proposed v1.6.0 per the decision memory) + CHANGELOG entry + npm publish.
- Optional follow-up (not v1.6.0): a CI nightly-diff job comparing scaffold's hard-coded skill names against `obra/superpowers/plugin.json` HEAD — would close the audit's "no drift detection" concern without forking. Mentioned in the decision memory.
- Optional follow-up: drop the dogfood file's legacy `additionalAllowList` shape and add `enabledPlugins["superpowers@claude-plugins-official"]: true` to it so we eat our own dog food (current legacy shape still triggers our own migration warning when re-run).

## Blockers / Risks
- Known limitation surfaced honestly per Rule 12 — `enabledPlugins` does NOT auto-install in headless `claude -p` mode (Anthropic issues #32606, #45323 closed as not-planned). Mitigation: 3 surfaces print the manual `/plugin install superpowers@claude-plugins-official` fallback (next-steps, session-start hook PS1, session-start hook SH).
- Decision is reversible by a one-commit revert (removes 3 JSON lines + 2 hook lines + 1 step line). Documented in the decision memory.
- Skill-name drift detection is NOT automated — future revisit trigger documented (`mem:decisions/superpowers-plugin-strategy` → "What revisits this decision").
