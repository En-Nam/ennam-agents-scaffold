# Checkpoint: tech-lead — 2026-06-26

## What was done
- Acted as Tech Lead under user-granted full autonomy ("OK, giao toàn quyền") to design + implement v1.8.0 `game-unity` profile (Unity 2.5D mobile).
- Brainstorming flow Q1-Q5 + D1-D11 with user (5 multi-select rounds); D12-D16 + §4 decided autonomously after handoff.
- Research workflow `wf_75e1331e-263` (9 agents, ~406k tokens, ~9 min) — picked CoplayDev Unity MCP v9.7.3, Tripo3D as default 3D asset gen (Pro tier blocker for commercial), omitted Sprite AI MCP, derived 10 Unity 2.5D conventions.
- Judge panel (3 advocates + 1 judge) picked Approach C — Big-bang + Safety Harness (over verified-first A and bare big-bang B).
- Wrote design spec `docs/superpowers/specs/2026-06-26-game-unity-profile-design.md` + decision memory `mem:decisions/game-unity-v1.8.0-bigbang-safety-harness` + backlog `mem:backlog/sprite-mcp-revisit-v1.8.x` + INDEX updated.
- Built `templates/game-unity/` profile from scratch: CLAUDE.md.partial (10 conventions + 8-phase workflow with Phase 6 Content Gates in marker block) + .mcp.json.partial (CoplayDev MCP) + README + 3 agents + 3 commands + 4 SKILL.md + Tripo fixture JSON + GDD/art-bible/perf-budget templates + 2 Editor-templates .cs + Editor-templates/README.
- Added `_shared/.gitattributes.append` (NEW — Unity LFS standard) + appended `.ennam/` to `_shared/.gitignore.append`.
- Wired CLI: `profiles.ts` (new entry), `wizard.ts` (new Game-Dev role + GameStack + resolveProfile 5th param), `ux.ts` (7 game-unity printNextSteps steps including Tripo dry-run mention per Judge objection #4).
- Tests: 2 modified (`profiles.test.ts`, `wizard.test.ts`) + 3 new (`game-unity-mcp-shape.test.ts`, `game-unity-skill-discipline.test.ts`, `profiles/game-unity.test.ts`).
- Wrote maintainer pre-publish script `scripts/verify-game-unity-bake.mjs` (5 checks: uvx present, PyPI knows coplay-mcp-server, Tripo base alive, Tripo balance via Python SDK if TRIPO_API_KEY set, package.json `files` excludes scripts/) — excluded from npm artifact.
- Wrote `CHANGELOG.md` (NEW file, v1.8.0 entry).
- Build + test green: 43 test files / 184 pass / 2 skipped (+4 files / +18 tests vs v1.6.0 baseline of 39/166).

## Files changed
- **CREATED (templates/game-unity/, 19)**: CLAUDE.md.partial.hbs, .mcp.json.partial.hbs, README.md, .claude/agents/{gameplay-engineer,asset-pipeline,build-and-test}.md, .claude/commands/{unity-greybox,unity-build-android,unity-content-gate}.md, .claude/skills/{unity-mcp-setup,unity-2.5d-conventions,perf-budget-check,asset-pipeline-tripo3d}/SKILL.md, .claude/skills/asset-pipeline-tripo3d/fixtures/tripo-image-to-model-success.json, GDD.md.hbs, art-bible.md.hbs, docs/perf-budget.md, Editor-templates/{EnnamPreflight.cs,EnnamPerf.cs,README.md}
- **CREATED (other)**: templates/_shared/.gitattributes.append, scripts/verify-game-unity-bake.mjs, CHANGELOG.md, docs/superpowers/specs/2026-06-26-game-unity-profile-design.md, tests/unit/game-unity-mcp-shape.test.ts, tests/unit/game-unity-skill-discipline.test.ts, tests/integration/profiles/game-unity.test.ts
- **MODIFIED**: templates/_shared/.gitignore.append, packages/cli/src/profiles.ts, packages/cli/src/wizard.ts, packages/cli/src/ux.ts, tests/unit/profiles.test.ts, tests/unit/wizard.test.ts
- **Serena MCP**: mem:decisions/game-unity-v1.8.0-bigbang-safety-harness, mem:backlog/sprite-mcp-revisit-v1.8.x, mem:INDEX (updated)

## Current state
- `npm run build`: ✅ tsup ESM 13ms
- `npm test`: ✅ 43 / 184 pass / 2 skipped — no regressions
- Git status: 6 modified + ~24 new files (all under templates/game-unity/, tests/, scripts/, docs/superpowers/specs/, CHANGELOG.md, _shared/.gitattributes.append). Not committed.
- `package.json` version: still `1.7.0` — DELIBERATELY NOT BUMPED (user authority per autonomy scope; bump + publish is the user's call).
- Maintainer pre-publish gate `scripts/verify-game-unity-bake.mjs` written but NOT executed — requires TRIPO_API_KEY env + network.

## Next steps
- (User) Review the design spec at `docs/superpowers/specs/2026-06-26-game-unity-profile-design.md` and the decision memory.
- (User) Run `node --import tsx scripts/verify-game-unity-bake.mjs` (or compile-then-run) with TRIPO_API_KEY set to verify the 5 checks against the live world.
- (User) If verify passes: bump `package.json` to `1.8.0` (+ packages/cli/package.json) → `git commit` → `git tag v1.8.0` → `npm publish`.
- (Future agent, if Coplay or Tripo API drift breaks tests/unit/game-unity-mcp-shape or skill discipline tests): update partial + bump pin per the snapshot-failure protocol.

## Blockers / Risks
- Tripo balance endpoint URL: skill avoids hard-coding (instructs Python SDK get_balance() or live OpenAPI schema fetch) — but the user's first `--live` invocation will hit reality; pre-publish verify is the safeguard.
- CoplayDev pip package `coplay-mcp-server` is asserted by the .mcp.json shape test but its existence on PyPI is asserted only by the maintainer script (network-dependent, not in CI).
- `EnnamPerf.cs` uses `Thread.Sleep` instead of EditorCoroutines as a template placeholder — documented in Editor-templates/README.md and SKILL.md caveats; production users should replace.
- Wizard adds new `Game-Dev` role + `resolveProfile` gains optional 5th param `gameStack` — backward-compatible (existing call sites unchanged) but tests `every resolved name is a registered profile` updated to cover the new branch.
