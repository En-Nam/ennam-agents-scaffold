# Checkpoint: cto (main session) — 2026-07-10

## What was done
1. **Planning round 2** — team Workflow (wf_f073806a) → 4 role-family workflow presets; filed #31-#33 +
   comments on #23/#26/#29/#30. `mem:decisions/v1.12-role-workflows`.
2. **Wave 0 SHIPPED** — #22 scanner-keyreport + #23 workflow-slot (byte-identical golden gate).
3. **Wave 1 SHIPPED** — #26 workflow selection (4 preset files + wizard step + `--workflow` flag +
   recommendWorkflow single/compose; doc-first roles now emit doc-first-signoff, data-analytics→data-insight,
   engineering byte-identical), #27 `--doctor` read-only engine (+`--json`, checks.ts shared detectors),
   #24 `docs/plugins.md` menu + printNextSteps/README pointer.

## Files changed (Wave 1)
- src new: doctor.ts, checks.ts. src edit: workflow.ts (catalog+recommend+assert), types.ts (EnumerateOptions.workflow),
  enumerate.ts (recommend), profiles.ts (data-analytics recommendedWorkflow), wizard.ts (chooseWorkflow),
  index.ts (--workflow/--doctor + checks.ts reuse), ux.ts (workflow hint + doctor + plugins pointers).
- templates/_shared/workflow/{doc-first-signoff,data-insight,quick-change,decision-brief}.md. docs/plugins.md.
- tests: unit/workflow-recommend, unit/doctor, integration/workflow-select-cli, integration/doctor-cli. CHANGELOG, README.

## Current state
- **`npm test` 316 pass / 64 files** (Wave 0 = 297, +19 Wave 1). tsc clean except 2 PRE-EXISTING analyze-claude errors.
- E2E verified: hr→Doc-First Workflow in CLAUDE.md; --doctor lists missing keys w/ URLs (exit 1); --doctor --json ok=true when keys set; --workflow override + bad-id exit 2.
- PR #34 (base main) = Waves 0+1; Closes #22/#23/#24/#26/#27 on merge. Branch pushed. version stays 1.11.1.

## Next steps
- Wave 2: #29 profiles-proofwave (ceo+ciso+design, needs #26✓) → #31 role-workflow pack (people-lifecycle/
  exec-decision/security-incident) → #32 finance-close → #33 accounting (gated on altered-number harness).
- P2: #25 settings-partial-infra, #28 OAuth spike (gates coo/cmo).

## Blockers / Risks
- Doc-first profiles' CLAUDE.md workflow section CHANGED (intended #26 behavior) — no test pinned it, all green.
- Byte-identity for engineering still gated by workflow-slot golden test.
- Doctor treats missing known-required key as ERROR (exit 1) — intended; CI must set keys or accept the report.
