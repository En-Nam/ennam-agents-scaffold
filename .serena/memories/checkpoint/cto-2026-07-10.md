# Checkpoint: cto (main session) — 2026-07-10

## What was done
Two phases on 2026-07-10:
1. **Planning round 2** — team Workflow (wf_f073806a) designing position-specific workflows for
   kế toán/HR/CEO/CFO/CISO. Critique cut 6 → 4 role-family presets. Filed issues #31-#33 + comments on
   #23/#26/#29/#30. Recorded `mem:decisions/v1.12-role-workflows`.
2. **Wave 0 IMPLEMENTED** (CTO: "implement wave 0 → PR → report → reply/close issues → sync serena"):
   - **#22 scanner-keyreport**: new `packages/cli/src/env-scan.ts` (`scanRequiredEnv`/`buildKeyReport`/
     `KEY_SOURCES`/`formatKeyReportStep`); `printNextSteps` now async + scanner-driven (per-key obtain-URL +
     guide + `.env.local` target, only still-missing, unknown tokens surfaced); `.gitignore.append` ignores
     `.env.local` + `.env`. Structured-config-only scan (no skills/** freeform).
   - **#23 workflow-slot**: lifted 7-phase text → `templates/_shared/workflow/engineering-full.md`; shared
     CLAUDE partial fills `{{{workflowSection}}}`; new `ProfileDef.recommendedWorkflow` (unset→engineering-full);
     new `packages/cli/src/workflow.ts` (`resolveWorkflowSrc`, fail-loud). Byte-identical golden test = merge gate.

## Files changed
- src: env-scan.ts (new), workflow.ts (new), types.ts, render.ts, execute.ts, enumerate.ts, ux.ts, index.ts.
- templates: _shared/workflow/engineering-full.md (new), _shared/CLAUDE.md.partial.hbs (slot), .gitignore.append.
- tests: unit/workflow-slot.test.ts, unit/env-scan.test.ts, integration/env-local-gitignore.test.ts,
  fixtures/claude-shared-block-engineering.golden.md. CHANGELOG (Unreleased v1.12 Wave 0).
- GitHub: issues #31-#33 + comments (#23/#26/#29/#30) [planning]; PR for Wave 0.
- Serena: decisions/v1.12-role-workflows, decisions/v1.12-enterprise-expansion (status note), INDEX, this checkpoint.

## Current state
- Build OK; **`npm test` 297 pass / 60 files** (+11). tsc clean except 2 PRE-EXISTING analyze-claude errors (Rule 3).
- Verified E2E: `dotnet-mvc` install prints key report (detected GITHUB_TOKEN already set → omitted; listed
  JIRA_URL/JIRA_TOKEN/PG_CONN_STR with links); `.gitignore` leads with the secret block. Golden byte-identity green.
- Branch claude/tool-enhancement-discussion-43syw9. version stays 1.11.1 (release bump deferred to batch cut).

## Next steps
- After PR merge: #22/#23 auto-close. Then Wave 1: #26 (presets pool + wizard step, needs #23) → #31 pack → #29 profiles → #27 doctor (needs #22).
- Do NOT bump package.json until a user-facing v1.12 batch is release-ready.

## Blockers / Risks
- Byte-identity of #23 is guarded by the golden test — if `_shared/CLAUDE.md.partial.hbs` or engineering-full.md
  is hand-edited, regenerate the golden deliberately (scratchpad/capture-golden.cjs) and re-verify diff=0.
- scanner is structured-config-only by design; keep skills/** out of scope or false positives return (negative test locks it).
