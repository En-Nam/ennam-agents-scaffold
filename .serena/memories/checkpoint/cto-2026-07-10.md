# Checkpoint: cto (main session) — 2026-07-10

## What was done (v1.12 enterprise-expansion — FULLY IMPLEMENTED across the day)
1. Planning rounds 1+2 (team Workflows) → 12 issues #22-#33.
2. **v1.12.0 SHIPPED + PUBLISHED to npm** (PR #34): #22 scanner, #23 workflow-slot, #24 plugins-menu,
   #26 workflow-selection, #27 --doctor, #29 ceo/ciso/design, #31 people-lifecycle/exec-decision/security-incident.
   Publish fix: Release tag pointed at pre-bump commit → merged bump PR #35 + workflow_dispatch on main → 1.12.0 live.
   (Open cleanup: v1.12.0 git tag still at old commit — GitHub tag-protection 403 blocks force-move; admin-gated.)
3. **v1.13.0 batch IMPLEMENTED** (this session, branch restarted from main): the 5 remaining issues —
   - #28 OAuth spike: Canva=static-emittable remote-http (`{"type":"http","url":"https://mcp.canva.com/mcp"}`, no
     token, interactive OAuth); Gmail=NOT emittable (guidance-only). `mem:decisions/v1.12-oauth-mcp-spike`.
   - #32 finance-close preset + #30's ops-review-cadence preset (in WORKFLOW_PRESETS).
   - #30 exec-expand: cto(github/exec-decision), coo(github/ops-review-cadence), cmo(canva/exec-decision) profiles;
     Executive sub-select grown to CEO/CISO/CTO/COO/CMO.
   - #33 accounting profile (Finance wizard role, autoPolicy, finance-close) + Rule-13 content-harness test.
   - #25 settings.json.partial.hbs per-profile merge (enumerate single+compose); agent-org SubagentStop now
     auto-installed (manual paste removed from printNextSteps).

## Files changed (v1.13.0)
- src: workflow.ts (+2 presets), profiles.ts (+cto/coo/cmo/accounting), wizard.ts (ExecRole+CTO/COO/CMO, Finance role),
  list.ts (PROFILE_ROLE), enumerate.ts (settings partial single+compose), ux.ts (canva note + agent-org auto-hook).
- templates: _shared/workflow/{finance-close,ops-review-cadence}.md; agent-org/.claude/settings.json.partial.hbs;
  cto/(9) coo/(9) cmo/(9, canva remote partial) accounting/(8) — authored by 4 parallel subagents.
- tests: unit/{settings-partial,exec-expand-wizard}.test.ts, integration/profiles/exec-expand-finance.test.ts,
  updated profiles.test.ts. CHANGELOG (Unreleased v1.13.0), README, packages/cli/README.
- Serena: decisions/v1.12-oauth-mcp-spike, decisions/v1.12-role-workflows (status), this checkpoint.

## Current state
- **`npm test` 341 pass / 69 files** (v1.12.0=326, +15). tsc clean except 2 PRE-EXISTING analyze-claude errors.
- E2E verified: --list groups Executive(5)+Finance; cmo → canva remote MCP + /mcp connect note; accounting → finance-close + POLICY.
- Branch pushed; PR for v1.13.0 to open. package.json stays 1.12.0 (release bump deferred to the tag/publish step).

## Next steps
- Open the v1.13.0 PR; on merge, close #25/#28/#30/#32/#33. Then release: bump 1.12.0→1.13.0, create Release v1.13.0
  (publish auto-fires). REMEMBER the tag lesson: the Release tag must point at the commit that already has the bumped version.
- Fix the stale v1.12.0 tag (admin force-move, still pending).

## Blockers / Risks
- Rule-13 finance "harness" is content-assertion (emit-config tool can't mock a live LLM) — documented honestly (Rule 12).
- 4 profile dirs authored by subagents; guardrails locked by tests, prose not line-by-line audited.
- cfo/cpo/chro remain cut (overlap pm/hr/finance-close+exec-decision).
