---
name: v1.12-enterprise-expansion
description: "CTO Danny's 4 asks (new org/C-level profiles, plugin research, workflow-picker, doctor tool) run through a team-agent round (research→design→critique→synthesis, workflow wf_db847c5b) → 9 GitHub issues #22-#30 across 4 dependency waves. Critique collapsed a 17-profile vision to a disciplined proof wave."
metadata:
  type: decision
  date: 2026-07-09
---

## Context
Danny (CTO) gave 4 asks for a v1.12 "enterprise expansion" and told the team to brainstorm/
research/critique together, then file issues. Ran a Workflow (9 agents: 4 design + 4 reviewer
critiques + 1 project-owner synthesis, ~639k tokens). Main session filed the issues to keep
title/label/VN-convention/assignee correct. All issues: label `enhancement`, assignee `danny-exnodes`.

## The 4 asks → 9 issues (#22-#30), 4 waves
- **Wave 0 (P0, no deps):** #22 `scanner-keyreport` (shared `scanRequiredEnv` + KEY_SOURCES + `.env.local`
  gitignore fix — Rule 13 keystone, feeds doctor+plugins); #23 `workflow-slot` (`{{{workflowSection}}}`
  slot + verbatim lift of CLAUDE.md.partial lines 44-128 → `engineering-full.md` + `ProfileDef.recommendedWorkflow` + diff=0 byte-identity gate).
- **Wave 1 (P1):** #26 `workflow-presets` (wizard step + `--workflow` + doc-first-signoff/data-insight, dep #23);
  #27 `doctor-cli` (`--doctor` read-only engine, dep #22); #24 `plugins-menu` (static docs/plugins.md, no deps).
- **Wave 2 (P1):** #29 `profiles-proofwave` (ceo+ciso+design doc-first, existing MCP only, Executive wizard
  role via QA-QC enum-throw sub-select, dep #26).
- **Wave 3 (P2, deferred):** #25 `settings-partial-infra` (`.claude/settings.json.partial.hbs` merge, no deps);
  #28 `mcp-oauth-spike` (Canva/Gmail static-emittability, dep #22); #30 `exec-expand` (cto now; coo/cmo gated
  on #28; dep #29+#28).

## Key critique-driven decisions (folded in)
- **17-profile / 5-epic vision → 3-profile proof wave** (ceo/ciso/design). Rule 2 + prior round's
  "ship 3 strong roles not many stubs". A doc-first profile = full template dir + Rule-9 tests (~0.5-1.5d each).
- **Byte-identity snapshot test is the MERGE GATE** for #23 (top mission risk — verbatim workflow lift).
- **Structured-config-only env scan** (`.mcp.json` + settings env blocks + tiny skill allowlist); NO freeform
  `.claude/skills/**` scan — verified false positives ($UNITY, $MESHY_API_KEY, $MAESTRO_CLOUD_API_KEY).
- **Doctor stays read-only** — only exec `claude --version`; no `claude plugin list`, no `~/.claude` probe.
- **Plugins as static docs/plugins.md** (user runs `/plugin install` themselves) — zero settings mutation;
  per-role `enabledPlugins` emission deferred (needs #25 first). Rule 7: figma/jira stay `@ennamjsc/*` wrappers.
- **Canva/Gmail/HubSpot/Notion/Slack profiles gated behind #28** (OAuth remote transport unproven as static config).
- **cpo/chro cut** (overlap shipped pm/hr; no sub-profile "mode" mechanism invented). Finance Rule-13 profiles
  (cfo/cro) OUT until code-derived-ground-truth skills + altered-number test mocks are scoped.

## Deviation from synthesis (surfaced — Rule 12)
Synthesis CUT `quick-change` + `decision-brief` presets. Main session KEPT them in #26 as **override-only,
never auto-recommended** — `decision-brief` (Frame→Options→Decide→Communicate) directly serves the C-level
decision audience the CTO emphasized; both are cheap phase-list text files. Flagged in #26's OUT-of-scope note.

## Verified code facts (Rule 13, before filing)
ux.ts:29-33 hardcoded envVars ✓; .gitignore.append omits .env.local ✓; render.ts noEscape global +
{{{profileSection}}} slot ✓; next uses @ennamjsc/figma-mcp ✓; partial has `### Superpowers Workflow`(44) →
`### Task Complexity Guide`(120) ✓.

## Cross-refs
Issues #22-#30. `mem:decisions/enterprise-foundation-v1.11` (v1.11 substrate this builds on),
`mem:decisions/superpowers-plugin-strategy` (why passthrough, not fork — reaffirmed by #24 Rule 7).
Research artifacts: /scratchpad/wf-{marketplace,taxonomy,workflow,doctor,synthesis}.json (session-local).
