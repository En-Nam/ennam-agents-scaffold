# Checkpoint: tech-lead — 2026-06-24

## What was done (two sessions, same day)

**Session 1 — v1.3.0** (committed 458c688, pushed to main):
- isMain npx silent-exit bug fix (realpath both sides of guard) + regression test
- Added 7 profiles (dotnet-mvc, express, ba, hr, devops-aws, devops-azure, devops-gcp) with agents + commands + skills + MCP partials (41 template files)
- Expanded wizard matrix to 5 roles × 6 dev stacks + 3 clouds
- Upgraded CLAUDE.md + AGENTS.md to v1.2.2 template power (mermaid, Serena MCP canonical, Rule 13)
- Added `.claude/skills/` classify rule
- README mermaid install-flow + v1.2→v1.3 upgrade note

**Session 2 — v1.4.0** (this session, ready to commit):
- Workflow `wa81d36at` (26 agents, 1.08M tokens, 15min): research → CTO→PO→BA→TL discussion pipeline → adversarial critic → 2 new profile generators + 13-profile refinement pass
- Added 2 new Developer stacks: `react` (Vite SPA — React 19 + React Router 7 + TanStack Query + shadcn/ui + Tailwind 4 + Vitest) and `react-native` (Expo SDK 52+ on New Architecture + Expo Router + NativeWind + Reanimated 3 + Maestro + EAS)
- Refinement pass: 7 files touched, 32 skipped (agents respected Rule 3 — surgical changes — most existing prompts already compliant)
- Implemented handoff prompt feature: `printHandoffPrompt(profileName)` in `ux.ts`, wired in `index.ts` post-install. BA-approved 30-line template with hard boundary rule (no edits inside markers), grounding rules (every claim cites a file), diff-before-write. Skipped for local-root + suppressed under --no-prompts.
- **Team disagreement → user override**: PO recommended deferring handoff prompt to v1.4.1 (split-reviewer-attention risk). User explicitly required shipping all 3 asks together. Decision recorded in `.serena/memories/decisions/v1.4-scope.md` addendum.
- Wizard: ReactJS + ReactNative added to Developer stacks
- README: mermaid flow updated, profile table extended, v1.3→v1.4 upgrade note covers both new stacks + handoff prompt feature

## Files changed (v1.4.0)

- packages/cli/src/{ux,index,profiles,wizard}.ts
- packages/cli/package.json, package.json (1.3.0 → 1.4.0)
- templates/react/ (6 files), templates/react-native/ (7 files) — new
- 7 refined files across python/qa/dotnet-mvc/hr/devops-aws/devops-gcp profiles
- tests/unit/{profiles,wizard,handoff-prompt}.test.ts (handoff-prompt = new)
- tests/integration/profiles/{react,react-native}.test.ts (new)
- README.md (mermaid + profile table + upgrade note)
- .serena/memories/{INDEX,decisions/v1.4-scope}.md

## Current state

**Ready to publish.** `npm run build` green (32.38 KB), `npm test` 156 passed + 2 skipped (Windows symlink). Working tree shows v1.4 modifications not yet committed.

## Next steps

1. Commit v1.4 + push to main.
2. `cd packages/cli && npm publish` — `prepublishOnly` rebuilds + retests.
3. Tag `v1.4.0`.

## Blockers / Risks

- Handoff prompt is a "behavior asked of another agent in another repo" with no automated test for the across-marker boundary contract — the prompt's wording is the only guardrail. Unit test asserts the prompt text contains the boundary rule; if an agent ignores the rule, the user's CLAUDE.md could be corrupted on first paste. Mitigation: diff-before-write requirement in the prompt + suppress under CI.
- Refinement was light (7 touched of ~40 files) — most existing prompts already met the rubric. If a reviewer wants more aggressive refinement, run a second pass with stricter criteria.
- React + RN profile content is mid-2026 stack-pinned (React 19 + Compiler 1.0, Expo SDK 52+, Reanimated 3) — pre-pin downstream churn will require a v1.5 refresh.
