# Checkpoint: tech-lead — 2026-06-24

## What was done

v1.3.0 release work — bug fix + major profile/role expansion:

1. **CLAUDE.md + AGENTS.md upgrade**: replaced stale v1.0.0-rc.0 boot block with current `templates/_shared/` content (mermaid Session Boot + Superpowers Workflow + Task Complexity Guide + Serena MCP canonical protocol + Read/Write mermaids). Added Rule 13 to root AGENTS.md.

2. **isMain bug fix** (`packages/cli/src/index.ts:183-200`): `pathToFileURL(argv[1])` did not resolve symlinks → under `npx`, the `node_modules/.bin/` symlink path never matched `import.meta.url` → `cli.parse()` never ran → silent exit 0. Fix: `realpathSync()` both sides. Regression test in `tests/integration/npx-symlink-bin.test.ts` (real symlink + npx-shaped node_modules/.bin layout; skipped on Windows where .cmd shims already point at realpath).

3. **Skills classify rule**: added `.claude/skills/` → `skip-if-exists` in classify.ts + test.

4. **7 new profiles** with full agent/command/skill/MCP packages (dispatched in parallel via Workflow tool, 14 agents, 363k tokens, 3min):
   - `dotnet-mvc`, `express` (Developer stacks)
   - `ba`, `hr` (single-profile roles)
   - `devops-aws`, `devops-azure`, `devops-gcp` (DevOps cloud subtypes)

5. **Wizard matrix expanded**: 5 roles (Developer, QA-QC, BA, HR, DevOps) × stacks (6 dev stacks) or clouds (3). BA/HR skip projectType/stack prompts; DevOps branches on cloud instead of projectType. Backward compat: every existing profile name still resolves.

6. **Version bump**: 1.2.0/1.2.2 → 1.3.0 (root + CLI). README rewritten with new Install flow ASCII, profile tables segmented by role, v1.2→v1.3 upgrade note.

## Files changed

- `CLAUDE.md`, `AGENTS.md` (project root upgrade)
- `packages/cli/src/{index.ts,classify.ts,profiles.ts,wizard.ts}` (bug fix + skills rule + new profiles + new matrix)
- `packages/cli/package.json`, `package.json` (1.2.x → 1.3.0)
- `README.md` (new profile tables + flow + v1.3 note)
- `templates/{dotnet-mvc,express,ba,hr,devops-aws,devops-azure,devops-gcp}/...` — 41 new template files
- `tests/unit/{classify,profiles,wizard}.test.ts` (expanded coverage)
- `tests/integration/npx-symlink-bin.test.ts` (NEW — isMain regression)
- `tests/integration/profiles/{dotnet-mvc,express,ba,hr,devops-aws,devops-azure,devops-gcp}.test.ts` (7 NEW)

## Current state

**Ready to publish.** `npm run build` green, `npm test` 134 passed + 2 skipped (Windows-only symlink skips by design), dry-run for `devops-aws` shows 28 ops with new agent/commands/skills correctly enumerated. Not yet committed.

## Next steps

1. User reviews diff against `main` and approves commit.
2. `git add` + commit (suggested message: `feat(v1.3.0): fix isMain npx bug + add 7 profiles (dotnet-mvc, express, ba, hr, devops-{aws,azure,gcp})`).
3. `cd packages/cli && npm publish` — `prepublishOnly` will rebuild + retest.
4. Tag `v1.3.0` on main.

## Blockers / Risks

- `@modelcontextprotocol/server-postgres` and `@modelcontextprotocol/server-github` are real reference MCPs — verified by using existing namespace patterns; users without `PG_CONN_STR` / `GITHUB_TOKEN` env vars will see the MCP fail to connect (loud, expected) rather than silent broken behavior. Follows the same env-driven pattern as existing `JIRA_TOKEN` / `FIGMA_TOKEN`.
- DevOps profiles assume Terraform-first; teams running pure CDK/Pulumi will need to adapt the agent. Documented in each profile's CLAUDE.md.
- No cloud-vendor MCPs added (AWS/Azure/GCP) — none have polished, stable npm packages. Used context7 (shared) + github (per profile) which cover IaC docs + repo workflows.
