# Checkpoint: cto — 2026-06-24

## What was done

Two CTO-driven workflow runs in one day:

**Run 1 (v1.1.0) — earlier:** Upgraded `_shared` templates (Rule 13, Serena MCP block, mermaid Session Boot, Task Complexity Guide). Added `local-root` profile. Added no-repo auto-detection. Renamed scope `@ennam` → `@ennamjsc`. Published to npm as `@ennamjsc/agents-scaffold@1.1.0`.

**Run 2 (v1.2.0) — later:** 14 commits on `worktree-wf_c6ca234e-8de-5`. Three deliverables:
1. **Interactive wizard** when invoked without profile arg: 2-3 step `@clack/prompts` flow (Role → ProjectType → Stack?) with matrix resolution to profile name. Extracted to `packages/cli/src/wizard.ts` to eliminate import side-effects in tests. Non-TTY guard added so `echo '' | npx ...` fails loud with exit 2 instead of hanging.
2. **README rewrite**: ASCII flow diagram (not mermaid — npm doesn't render mermaid), badges, wizard-first quickstart, Claude for Chrome subsection, explicit v1.1→v1.2 migration note about the orphan `chrome-devtools` entry that user-wins mergeJson can't remove.
3. **Chrome DevTools MCP → Claude for Chrome browser extension**: removed `chrome-devtools` from `next` and `qa` extraMcp arrays; deleted `templates/qa/.mcp.json.partial.hbs`; trimmed `templates/next/.mcp.json.partial.hbs`; updated profile partials' prose to recommend the Claude for Chrome extension.

Adversarial review found 4 majors (wizard side-effects in tests, mermaid not rendering on npm, no v1.1 migration note, silent chrome-devtools carryover). All 4 fixed plus 3 minors (initialValue defaults, intro-before-validation footgun, duplicate-marker clean stderr).

## Files changed

v1.1: 24 files / +570/-58. v1.2: 21 files / +461/-44. New CLI module `packages/cli/src/wizard.ts`. New integration tests: `v11-migration.test.ts`, `duplicate-marker-cli.test.ts`, `no-prompts-missing-profile.test.ts`. New unit test: `wizard.test.ts`.

## Current state

**Working — v1.1.0 LIVE on npm; v1.2.0 ready to merge.** v1.1 main is at `3f8c364`. v1.2 worktree clean at `fb5c53a` on `worktree-wf_c6ca234e-8de-5`. Build green, 109/109 tests pass (was 93 in v1.1; +16 for migration + wizard + duplicate-marker contracts), idempotency holds, chrome-devtools removed end-to-end (grep tmpdir → 0 matches for next + qa). NOT yet merged to main; awaiting user authorization.

## Next steps

1. User reviews v1.2 diff (`git -C .claude/worktrees/wf_c6ca234e-8de-5 diff main..HEAD`) and approves ff-merge to main.
2. After merge: `git worktree remove .claude/worktrees/wf_c6ca234e-8de-5 --force`, then `cd packages/cli && npm publish` (prepublishOnly rebuilds templates).
3. Optional follow-ups: tag `v1.1.0` and `v1.2.0` on main; consider adding `license` field to package.json so npm sidebar matches README badge; decide whether QA-QC branch should skip the projectType prompt (spec deviation — needs product call).

## Blockers / Risks

None blocking. v1.1 publish on npm is permanent (1.1.0 slot is consumed regardless of v1.2 outcome). Three knowingly-deferred items from v1.2 review: (a) `package.json` license-field cosmetic mismatch with README badge; (b) QA-QC's projectType prompt is asked but ignored (matches locked spec; relabeled to clarify only `.gitignore` impact); (c) optional CLI runtime warning for stale `chrome-devtools` entry was added; could be made louder.
