# Checkpoint: tech-lead — 2026-06-25

## What was done — v1.5.0 (devops-docker profile)

Two workflows executed today:

**R&D workflow `wx7zocqy1`** (6 agents, 238k tokens, ~7min) — surveyed Docker remote-management OSS landscape:
- 5 parallel research streams (Portainer CE/BE, lightweight alternatives, specialized tools, Tailscale+Docker patterns, build-vs-buy + socket security) → 28+ official sources
- Synthesis verdict: BUY not BUILD (custom UI = 24 agent-weeks, unjustifiable). Default stack: Portainer CE + socket-proxy + Tailscale + specialized observability sidecars.
- User answered the 3 open questions: GitOps strict + Renovate PR-based + >3 hosts/team>5 → pivoted PRIMARY from Portainer to **Komodo** (Resource Sync TOML-as-truth, native RBAC + audit, no node-limit gate)

**Build workflow `wctt1xpjl`** (4 agents, 215k tokens, ~15min) — generate → adversarial review → refine → verify:
- Generated 11 files for `devops-docker` profile
- Critic found 15 findings (1 blocker, 5 major, 9 minor) — including a network_mode typo that pointed at the wrong sidecar, internal inconsistency between agent and skill on Komodo TOML field paths, and an incomplete validation grep
- Refiner applied all 15 surgically. Agent file compressed 3906 → 2047 bytes (under 2KB rubric)
- All sanity checks pass: agent mentions docker-socket-proxy + Komodo + Tailscale; boundaries forbid raw socket bind + :latest + Watchtower; compose-stack-discipline skill encodes healthcheck + restart + no :latest

## Files changed

- `packages/cli/src/profiles.ts` — added `devops-docker` entry (extraMcp: ['github'])
- `packages/cli/src/wizard.ts` — added 'Docker' to Cloud union; CLOUD_TO_PROFILE map; wizard option labeled "Docker (self-hosted; Komodo + Tailscale)"; renamed prompt to "Which cloud / infra target?"
- `packages/cli/package.json`, `package.json` — 1.4.0 → 1.5.0
- `templates/devops-docker/` (NEW) — 11 files: CLAUDE.md.partial.hbs, .claude/agents/devops-docker.md (2047 bytes), 4 commands (docker-stack, docker-up, tailscale-route, komodo-sync), 4 skills (docker-socket-proxy-acl, tailscale-sidecar-pattern, compose-stack-discipline, komodo-resource-sync-gitops), .mcp.json.partial.hbs
- `tests/unit/{profiles,wizard,handoff-prompt}.test.ts` — coverage for devops-docker entry + DevOps→Docker matrix branch
- `tests/integration/profiles/devops-docker.test.ts` (NEW) — verifies install + architecture coherence (agent must mention the 3 load-bearing components + forbid the 3 anti-patterns) + agent file ≤2048 bytes regression guard
- `README.md` — mermaid flow gains Docker branch; DevOps table grows; v1.4→v1.5 upgrade note explains the locked architecture

## Current state

**Ready to publish.** `npm run build` clean (32.76 KB), `npm test` 160 passed + 2 skipped (Windows symlink). Working tree contains v1.5 changes not yet committed.

## Next steps

1. Commit + push to main.
2. `cd packages/cli && npm publish` after `npm login` (v1.4.0 publish failed with 401 — token expired; user needs to re-login first).
3. Tag `v1.5.0`.

## Blockers / Risks

- Adversarial critic caught architecture-coherence bugs that generate phase missed (cross-file references like sidecar naming). Quality pattern works but adds ~2× wall-clock vs generate-only. Acceptable trade-off; baked into ultracode default.
- Komodo's GPL-3.0 license is more restrictive than Portainer's zlib — fine for end-user homelab but documented in agent prompt as a heads-up if user later redistributes.
- The `@modelcontextprotocol/server-github` npm package is the OLD reference server; critic recommended switching to `ghcr.io/github/github-mcp-server` (Go binary in container). For consistency, all devops-* profiles still use the npm form. Future v1.6 cleanup: migrate every devops-* profile to the Go-based github MCP at once.
