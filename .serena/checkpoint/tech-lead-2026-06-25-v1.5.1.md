# Checkpoint: tech-lead — 2026-06-25 (v1.5.1)

## What was done

Patched two legacy `.claude/settings.json` shapes that the scaffold has been shipping since v1.0 and that current Claude Code rejects:

1. `permissions.additionalAllowList` → renamed to `permissions.allow` in the template. The legacy key was silently ignored — users' allow-rules for `Bash(npm:*)` etc. never took effect.
2. `hooks.SessionStart` bare `[{command}]` → wrapped in the required `[{hooks: [{type:"command", command}]}]` shape. The bare shape triggers Claude Code's "Expected array, but received undefined" parse-error popup.

Auto-rewrite is not possible because `mergeJson` is user-wins on arrays. Added `maybeWarnLegacySettings(cwd)` in `packages/cli/src/index.ts` that detects either legacy shape on install and warns loudly on stdout. Pattern mirrors the existing `maybeWarnStaleChromeDevtools`. Wired in after `maybeWarnStaleChromeDevtools` and before the handoff prompt.

Caught a logic bug during integration testing: the warner originally guarded `additionalAllowList` warning with `!('allow' in perms)`, which suppressed the warning whenever the merged result had BOTH keys (which is exactly what happens on a v1.5.0 → v1.5.1 re-run). Test caught it before ship; relaxed the condition.

## Files changed

- `templates/_shared/.claude/settings.json.hbs` — correct shape (allow + nested hooks)
- `packages/cli/src/index.ts` — added `maybeWarnLegacySettings(cwd)` helper + call site
- `packages/cli/package.json`, `package.json` — 1.5.0 → 1.5.1
- `tests/integration/legacy-settings-warn.test.ts` (NEW) — 3 tests: template ships correct shape + warner detects pre-1.5.1 broken install + no false-positive on already-correct shape
- `README.md` — v1.5.0 → v1.5.1 upgrade note
- `d:/Projects/EnNam/ennam-auto-pipeline/.claude/settings.json` — local fix (user opened the file in VS Code, hit the parse-error popup that surfaced this whole bug)

## Current state

**PUBLISHED to npm.** Verified via `npm view @ennamjsc/agents-scaffold time --json` (2026-06-25T04:24Z):

| Version | npm publish time (UTC) | npm publish time (ICT) | Status |
|---|---|---|---|
| 1.4.0 | 2026-06-25 02:42:20Z | 2026-06-25 09:42 | LIVE on npm |
| 1.5.0 | 2026-06-25 03:42:48Z | 2026-06-25 10:42 | LIVE on npm |
| 1.5.1 | 2026-06-25 04:22:44Z | 2026-06-25 11:22 | LIVE on npm — latest |

`npm view @ennamjsc/agents-scaffold version` returns `1.5.1` → `npx @ennamjsc/agents-scaffold` now installs the v1.5.1 patched template (correct `permissions.allow` + nested `hooks.SessionStart` shape).

Build state: `npm run build` clean (34.28 KB), `npm test` 163 passed + 2 skipped (Windows symlink). Main branch HEAD = `9766c01` (matches what's published).

## Next steps

1. (Optional) Tag the three releases on git: `git tag v1.4.0 && git tag v1.5.0 && git tag v1.5.1 && git push origin v1.4.0 v1.5.0 v1.5.1`. Earlier checkpoints planned the tags but they were not created — `git tag -l` will confirm. Not blocking; npm registry is the source of truth for the published artifacts.
2. Watch `npm view @ennamjsc/agents-scaffold` download counts over the next 30 days to see whether the legacy-settings warner is firing for real-world upgraders (proxy signal: GitHub issues mentioning the warning text).
3. The local fix in `d:/Projects/EnNam/ennam-auto-pipeline/.claude/settings.json` stopped Claude Code's parse-error popup at the time of the bug surface; verify it still holds after the v1.5.1 publish.

## Blockers / Risks

- Users on v1.0–v1.5.0 who never re-run the scaffold will keep the broken shape until they manually update. Warning only fires on a NEW scaffold run, not at Claude Code startup. Acceptable — re-running is the natural upgrade trigger. Re-runs against the now-live 1.5.1 will fire the warner.
- Scaffold's own `.claude/settings.json` (dogfooded install) still has `additionalAllowList`. Decided NOT to update it in this release to keep the diff focused on the template + warner. Will get fixed naturally when scaffold is re-installed on itself in a later session — or explicitly in a v1.5.2 hygiene patch.
- Three sequential publishes in ~100 minutes (1.4.0 → 1.5.0 → 1.5.1) on 2026-06-25 — npm permits this; no rate-limit issues observed. If a future session needs to publish multiple versions in one burst, the same pattern works.
